import { stitches } from '../../../../stitches.config';

const { styled } = stitches;

export const LabelContainer = styled('label', {
  textAlign: 'left',
  display: 'block',
  fontWeight: '$md',
});
